<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>DO English Now – Learn English the Fun British Way</title>
<meta name="viewport" content="width=device-width, initial-scale=1">

<style>
  :root {
    --uk-blue: #00247D;
    --uk-red: #CF142B;
    --uk-white: #FFFFFF;
    --uk-gold: #FFD700;
    --duo-green: #A8E6A3;
    --bg: #F7F9FB;
    --text-dark: #1A1A1A;
    --radius: 22px;
    --shadow: 0 8px 20px rgba(0,0,0,0.08);
  }

  * { box-sizing: border-box; }

  body {
    margin: 0;
    font-family: "Nunito", system-ui, sans-serif;
    background: var(--bg);
    color: var(--text-dark);
  }

  a { text-decoration: none; color: inherit; }

  header {
    background: var(--uk-blue);
    color: white;
    padding: 0.8rem 1.5rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
    position: sticky;
    top: 0;
    z-index: 10;
  }

  .logo-area {
    display: flex;
    align-items: center;
    gap: 0.75rem;
  }

  .logo-area img {
    height: 44px;
    border-radius: 10px;
  }

  .logo-text {
    font-size: 1.5rem;
    font-weight: 800;
  }

  nav {
    display: flex;
    gap: 1rem;
    font-weight: 600;
    font-size: 0.95rem;
  }

  nav a {
    padding: 0.4rem 0.7rem;
    border-radius: 999px;
  }

  nav a.active,
  nav a:hover {
    background: rgba(255,255,255,0.15);
  }

  .page {
    max-width: 1100px;
    margin: auto;
    padding: 2.5rem 1.5rem 3.5rem;
  }

  .two-col {
    display: grid;
    grid-template-columns: minmax(0, 1.4fr) minmax(0, 1fr);
    gap: 2rem;
    align-items: flex-start;
  }

  .hero-title {
    font-size: 2.4rem;
    font-weight: 900;
    color: var(--uk-blue);
    margin: 0 0 0.5rem;
  }

  .hero-sub {
    margin: 0 0 1.5rem;
    font-size: 1.05rem;
  }

  .btn-primary {
    background: var(--uk-red);
    color: white;
    padding: 0.8rem 1.4rem;
    border-radius: var(--radius);
    font-weight: 700;
    border: none;
    box-shadow: var(--shadow);
    cursor: pointer;
  }

  .btn-secondary {
    background: white;
    color: var(--uk-blue);
    padding: 0.7rem 1.2rem;
    border-radius: var(--radius);
    font-weight: 700;
    border: 2px solid var(--uk-blue);
    cursor: pointer;
  }

  .btn-small {
    padding: 0.4rem 0.9rem;
    font-size: 0.85rem;
  }

  .btn-row {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    margin-bottom: 1.5rem;
  }

  .mascot-card {
    background: white;
    border-radius: 26px;
    padding: 1.5rem;
    box-shadow: var(--shadow);
    text-align: center;
  }

  .mascot {
    width: 90px;
    height: 90px;
    border-radius: 50%;
    background: var(--duo-green);
    margin: 0 auto 0.75rem;
    position: relative;
  }

  .mascot::before,
  .mascot::after {
    content: "";
    position: absolute;
    top: 30%;
    width: 18px;
    height: 18px;
    border-radius: 50%;
    background: white;
  }

  .mascot::before { left: 22%; }
  .mascot::after { right: 22%; }

  .mascot-eye-dot {
    position: absolute;
    top: 36%;
    left: 26%;
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: black;
    box-shadow: 32px 0 0 0 black;
  }

  .mascot-mouth {
    position: absolute;
    bottom: 25%;
    left: 50%;
    width: 40px;
    height: 20px;
    margin-left: -20px;
    border-radius: 0 0 40px 40px;
    border-bottom: 4px solid #14532d;
  }

  .stats-row {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    margin-bottom: 1.5rem;
  }

  .stat-pill {
    background: white;
    border-radius: 999px;
    padding: 0.6rem 1rem;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    box-shadow: var(--shadow);
    font-size: 0.9rem;
  }

  .stat-label {
    font-weight: 700;
  }

  .stat-value {
    font-weight: 800;
    color: var(--uk-red);
  }

  .xp-bar {
    height: 14px;
    background: #E0E0E0;
    border-radius: 999px;
    overflow: hidden;
    margin-top: 0.4rem;
  }

  .xp-fill {
    height: 100%;
    width: 60%;
    background: linear-gradient(90deg, var(--uk-red), var(--uk-gold));
  }

  .map-section-title {
    font-weight: 800;
    margin-bottom: 0.75rem;
  }

  .lesson-map {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 1rem;
  }

  .lesson-node {
    background: white;
    border-radius: 999px;
    padding: 0.8rem 0.6rem;
    text-align: center;
    box-shadow: var(--shadow);
    font-size: 0.9rem;
    border: 2px solid var(--duo-green);
    position: relative;
  }

  .lesson-node.locked {
    opacity: 0.5;
    border-style: dashed;
  }

  .lesson-node span {
    display: block;
    font-weight: 700;
    color: var(--uk-blue);
  }

  .lesson-node small {
    font-size: 0.75rem;
  }

  .lesson-node .level-badge {
    position: absolute;
    top: -8px;
    right: 10px;
    background: var(--uk-gold);
    color: #111;
    font-size: 0.7rem;
    padding: 0.1rem 0.4rem;
    border-radius: 999px;
    font-weight: 800;
  }

  .panel {
    background: white;
    border-radius: 24px;
    padding: 1.5rem;
    box-shadow: var(--shadow);
    margin-top: 2rem;
  }

  .panel h2 {
    margin-top: 0;
    font-size: 1.3rem;
    color: var(--uk-blue);
  }

  .panel h3 {
    margin-top: 0;
    font-size: 1.05rem;
  }

  .panel-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    gap: 1.25rem;
  }

  .small-card {
    background: #FDFDFE;
    border-radius: 18px;
    padding: 1rem;
    border: 1px solid #E5E7EB;
  }

  .small-card strong {
    display: block;
    margin-bottom: 0.25rem;
  }

  .form-grid {
    display: grid;
    grid-template-columns: minmax(0, 1.1fr) minmax(0, 1fr);
    gap: 2rem;
  }

  form label {
    display: block;
    font-size: 0.9rem;
    margin-bottom: 0.25rem;
    font-weight: 600;
  }

  form input,
  form select,
  form textarea {
    width: 100%;
    padding: 0.6rem 0.7rem;
    border-radius: 12px;
    border: 1px solid #D1D5DB;
    font-family: inherit;
    font-size: 0.95rem;
    margin-bottom: 0.9rem;
  }

  form textarea {
    min-height: 80px;
    resize: vertical;
  }

  footer {
    background: var(--uk-blue);
    color: white;
    padding: 1.5rem;
    text-align: center;
    font-size: 0.9rem;
  }

  footer img {
    height: 32px;
    display: block;
    margin: 0 auto 0.5rem;
  }

  .view {
    display: none;
  }

  .view.active {
    display: block;
  }

  @media (max-width: 800px) {
    .two-col,
    .form-grid {
      grid-template-columns: 1fr;
    }
    .lesson-map {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }
</style>
</head>

<body>

<header>
  <div class="logo-area">
    <img src="logo.png" alt="DO English Now Logo">
    <div class="logo-text">DO English Now</div>
  </div>
  <nav>
    <a href="#" data-view="student" class="active">Student</a>
    <a href="#" data-view="teacher">Teacher</a>
    <a href="#" data-view="auth">Login / Sign up</a>
  </nav>
</header>

<main id="student" class="view active">
  <div class="page">
    <div class="two-col">
      <div>
        <h1 class="hero-title">Welcome back, learner!</h1>
        <p class="hero-sub">
          Keep your streak alive, earn XP, and unlock new British English lessons with DO English Now.
        </p>

        <div class="stats-row">
          <div class="stat-pill">
            <span class="stat-label">Streak</span>
            <span class="stat-value">7 🔥</span>
          </div>
          <div class="stat-pill">
            <span class="stat-label">Today’s XP</span>
            <span class="stat-value">45 XP</span>
          </div>
          <div class="stat-pill">
            <span class="stat-label">Total XP</span>
            <span class="stat-value">1,230</span>
          </div>
        </div>

        <div>
          <strong>Level 3 – Confident Speaker</strong>
          <div class="xp-bar">
            <div class="xp-fill"></div>
          </div>
          <small>60% to Level 4</small>
        </div>

        <div class="panel" style="margin-top:1.5rem;">
          <h2>Lesson map</h2>
          <p class="map-section-title">Tap a lesson to start:</p>
          <div class="lesson-map">
            <div class="lesson-node">
              <div class="level-badge">A1</div>
              <span>Basics 1</span>
              <small>Greetings & names</small>
            </div>
            <div class="lesson-node">
              <div class="level-badge">A1</div>
              <span>Basics 2</span>
              <small>Family & home</small>
            </div>
            <div class="lesson-node">
              <div class="level-badge">
